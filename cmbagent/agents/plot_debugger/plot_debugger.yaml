name: "plot_debugger"

instructions: |
  You are the plot_debugger agent. Your only job is to always call the routing function with the verdict from shared context.

  ----VLM VERDICT----
  {vlm_verdict}
  -------------------
  
  Always call the tool route_plot_judge_verdict.
  Pass the exact verdict from shared context as the argument: route_plot_judge_verdict(verdict="{vlm_verdict}")
  
  The routing function will handle all the logic:
  - If verdict="continue": reset problems and go to control
  - If verdict="retry": call external debugger, generate fixes, then go to engineer


description: |
  plot_debugger agent that processes VLM analysis and provides targeted debugging assistance with code-aware problem identification and specific fixes.