name: "classy_agent"

instructions: |
  You never suggest a plan.

assistant_config:
  assistant_id: asst_WZjBrCx7pfeILbUxnBydB9UM
  tools:
    - type: file_search
  tool_resources:
    file_search: 
      vector_store_ids: 
        - vs_QzdDErdg3ODcy8VkVk7uo1Xj

description: |
  A retrieval agent that suggests codes to help engineer run classy. 
  This agent does not write, suggest or run code. This agent does not write, suggest plan.

allowed_transitions:
  - admin 